{"author":{"id":"3f8bad4585cb0f07c6570d9e31f06264af3b0abf48efbc5a44f56df5e7a9cd45"},"ops":[{"type":3,"timestamp":1789401550,"nonce":"dijO7gTuyVOiLuU2usz1YwU0O2A=","message":"Sequences, landed as 05ee4c7 and 015db61 (200k iterations, load ~6), ns per element:\n\n| direction | before | after |\n|---|---|---|\n| Haxe→Wren (a Haxe array walked by Wren's for) | 264 | 83 |\n| Wren→Haxe (a Wren list walked by a Haxe for over caribou.Sequence) | 332 | 102 |\n\nWhat the profile of each showed:\n\n- Haxe→Wren: every len/index did find_method for getDyn, field_of for length and find_method for get_length again, each an hlp_lookup_find walk up the runtime object chain with a field hash read under an RwLock. Ash keeps the shape per hl_type now (a lock-free list like kinds_for's) and calls getDyn/setDyn by their kinds; a field hash is the symbol's own. 264 → 83.\n- Wren→Haxe: every count/[_] send interned its signature text and its static: twin (SipHash, a format! allocation) through has_method then send. The six fixed signatures are interned once per VM now (Signatures::fixed). 332 → 167.\n- Then each of those sends ran under the guard because the thread was under the fiber loop's: a setjmp and trap per element. enter sets the guards aside for a site's unguarded run now (05ee4c7, with a test for a throw two runs deep). 167 → 142.\n- Then Std.isOfType(this, Array) twice per element: Array's BaseType has four implementers, each a safeCast walk, after a cast to BaseType. The iterator decides once, and the single-access paths ask ArrayBase, one walk. 142 → 102. An uninitialised field of an inlined constructor is an unassigned HashLink register, which is not cleared: both fields of the iterator start null explicitly.\n\nLeft per element on Wren→Haxe: the native call itself (import::entry, value_to_word), the Dynamic result boxed then cast to T, and the Wren dispatch (Args::cross, dispatch_method, list_subscript). On Haxe→Wren: two host calls per element (iterate, iteratorValue), each a full crossing.","files":null}]}