{"author":{"id":"3f8bad4585cb0f07c6570d9e31f06264af3b0abf48efbc5a44f56df5e7a9cd45"},"ops":[{"type":3,"timestamp":1789227471,"nonce":"LY5E1nYI0rXbnbrnWMIMTualOX0=","message":"Ash side of the scheduler landed on branch caribou-heap (worktree\n.claude/worktrees/caribou-heap, uncommitted; this step: 9 files,\n+268/-1143). std/src/fiber.rs is 628 lines, down from 1514: the adapter\nlayer (closure runner, stub resolver, compiled-worker gate, lock-owner\nidentity, thread_create, the nine #[no_mangle] exports with identical\nshape) over caribou::sched.\n\nContracts met: per-fiber trap chain and exception value as a FiberHost\nHostState attached from inside each task body and to TaskId::NONE per\nthread; the interpreter's C switch hook bridged behind the core hook, so\nit runs after the sp publish; poll epoch is the core's (ash_fiber_poll_epoch\ndeleted, LLVM static-AOT names caribou_poll_epoch, everything else goes\nthrough the getter; verified with nm and an --emit-aot object); worker\nplacement unchanged; fibers_active -\u003e any_live_tasks (process-wide, so a\nmain thread whose tasks are all pooled still waits instead of declining).\nstd/Cargo.toml pins krio-fiber to caribou's rev: two krio copies in one\nbinary break the shared wasm suspender.\n\nVerified vs main's binary: parity 110/110 byte-identical; test_mpsc and the\nHaxe threads program under default/ASH_WORKERS=0/ASH_WORKERS=4/\nASH_GC_STRESS=1 in both modes, identical; sys program identical (its 19\nfailures are pre-existing on both). ash_std 115 tests, ash_core 60. Zero\nwarnings. threads.hl timings within noise.\n\nOpen: wasm+atomics pooled bodies run through the core's RunThrough and\ncannot suspend through ash's transform (needs a core hook for an\nadapter-provided pooled body; local wasm threads keep the guest Fiber);\nthe switch hook is per-world, so a foreign thread calling thread_create no\nlonger runs the interpreter's hook (that was a race before).","files":null}]}