{"author":{"id":"3f8bad4585cb0f07c6570d9e31f06264af3b0abf48efbc5a44f56df5e7a9cd45"},"ops":[{"type":3,"timestamp":1789331799,"nonce":"liPdUf6EmgtQx3BhLP6WT3reRFg=","message":"Direct sends (7490468) and the allocation path (db8aa53), each cell\nalone, ns per call:\n\n                Haxe→Haxe  Wren→Wren  Haxe→Wren  Wren→Haxe\n    static call       3.6        3.5       30.0       60.9\n    method call       3.6        5.8       33.3       65.8\n    getter            3.5        3.6       19.8       34.9\n    setter            0.7        3.7       23.2       34.5\n    closure call      3.6        3.6       74.4       67.7\n    construct         7.5       32.5      268.8      253.5\n\nConstruct inside Wren was 59 because a hosted allocation took the GC\nlock twice, looked its size up in the side table, and every cycle looked\nit up again per pin; now 26 in the runner (wren_lift's own heap: 5.3),\nthe rest being the core's trigger at 8 MB and wren_lift's\n`should_collect` per allocation.\n\nWhere a call goes now, from `sample`, ns of the cell:\n\nHaxe→Wren static (30): Ash's record call ~3; the entry and its word\nconversions ~3; the bridge ~2; the direct send ~18, of which the\nthread-local VM read ~4, the class check ~1, the argument buffer ~2 and\nwren_lift's `dispatch_method` ~10 (two thread-local reads,\n`modvars_cell_addr`, stats); the body ~3.\n\nWren→Haxe static (61): wren_lift's native call machinery\n(`wren_call_1_inner`, `dispatch_call`, `dispatch_method`, three\nthread-local reads) ~16; the trampoline's glue (record, binding, Rc,\nbuffers, conversions) ~10; the bridge ~4; placing the arguments by kind\n~7; the HashLink trap ~15 (`hlp_setup_trap_jit` and `hlp_remove_trap_jit`\ngo through a RefCell thread-local each, plus `gc_lock_held_depth` and\n`_setjmp`); the call and body ~4.\n\nWhat is left is mostly the runtimes' own entry costs. To reach baseline\nplus ten:\n\n- wren_lift: a call entry for a found compiled method that takes the\n  thread's JIT state once and skips the stats and the module-var lookup\n  (`dispatch_method` is ~10 of the 30); a native method kind that carries\n  a context word, so a bound member reaches its target without a class\n  lookup and the `dyn NativeContext` cast (~5 of the 61); a cheaper\n  native call path than `wren_call_N` → `dispatch_call` →\n  `dispatch_method` (~16 of the 61).\n- Ash: a trap whose setup and removal are a pointer push and pop on one\n  thread-local rather than a RefCell borrow each way (~10 of the 61).","files":null}]}