{"author":{"id":"3f8bad4585cb0f07c6570d9e31f06264af3b0abf48efbc5a44f56df5e7a9cd45"},"ops":[{"type":3,"timestamp":1789498697,"nonce":"Qsp2SDdYB/4nyXzT7LLPpHw9HFs=","message":"Closed 2026-09-15 with caribou 0a18e28 and wren_lift da9b6c1.\n\nThe mechanism landed with the one-world work: the core's stop runs\nwren_lift's `host_stop(true)` through the stop hook, which holds every\npoll page unreadable, so a compiled loop faults at its header into\nwren_lift's safepoint and passes through the seam's `thread_safe` and\n`thread_running`, where the core holds it. Both code generators poll\nthe page (Cranelift at loop headers; the AOT names the static page by\nsymbol), so the LLVM tier is covered by the same slot.\n\nWriting the test (tests/stop.rs: a tiered method spinning on numbers\non a pool worker; three `heap::major()` from the main thread, each\nasserted under 500 ms against the 2 s rendezvous deadline) found that\nit passed with the hook disabled too: the worker was never waited for.\nwren_lift's `stop()` enters the safe state through the seam\n(`enter_safe` → `thread_safe`, core blocking depth +1) but made the\ncollector running by a store alone, so `resume` never told the seam:\nevery cycle, `stop_world` and tier install left the calling thread one\nlevel \"blocking\" in the core for good, never stopped and scanned at a\nstale sp. da9b6c1 has `resume` tell the seam (`thread_running`). With\nit the worker reads blocking=0 at a stop, the test fails without the\nhook (a collection takes 2.02 s, abandoned) and passes with it.","files":null}]}