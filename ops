{"author":{"id":"3f8bad4585cb0f07c6570d9e31f06264af3b0abf48efbc5a44f56df5e7a9cd45"},"ops":[{"type":3,"timestamp":1789757180,"nonce":"RKp/RBrm/185slCsQnnwTmBWD0w=","message":"4315453768b7882c611aec2f6a5e854657a3ebae: the host table. `caribou_plugin_entry(*const Host)`; `Text` (one word, the core Str's address; header spelled as `TextData`, offset asserted against `Str::LEN_OFFSET`) as BYTES param/result; `Kept` over heap handles; `host::call` via `Callable::Dynamic`; `raise`/`raise_value` set pending and the dispatcher answers RAISED when a pending error is there after the call. Two-word (ptr,len) by value was rejected: not one word for `ash_native_call`, and passed by hidden pointer on Windows x64. NUL-terminated rejected: core strings are not, so it would copy. Verified from Wren and Haxe: strings both ways, a kept Haxe/Wren closure called from a plugin object, the closure's error propagating to the caller. Left: byte buffers, NativeLib bundle sections, wren_lift's own plugins on the ABI.","files":null}]}