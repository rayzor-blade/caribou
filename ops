{"author":{"id":"3f8bad4585cb0f07c6570d9e31f06264af3b0abf48efbc5a44f56df5e7a9cd45"},"ops":[{"type":3,"timestamp":1789485050,"nonce":"veqs3sOuxxvy5jbCNMJkZ1ZXvK4=","message":"Reload, first increment, landed in 8354408 (Wren spokes under a Haxe\ndriver):\n\n- `World::reload(namespace, module)` -\u003e `Adapter::reload` -\u003e epoch bump\n  -\u003e `Event::Reload`; `World::on(kind, handler)` subscribes, `raise` and\n  `deliver` run handlers from `tick` and after a reload. `Session::reload`\n  does it with the VM entered. `Event` has the one kind anything raises.\n- caribou-wren: `project::reload` = `VM::reload_module` (wren_lift\n  1d2f16f: class identity kept, compiled bodies dropped, inline caches\n  cleared) + `publish_module` + `VM::notify_reloaded` (Hatch.onReload).\n- Epoch: one process-wide `protocol::epoch()`, stamped into every\n  `CallSite` by `set`/`set_direct` and checked by `get`/`direct`, rather\n  than one per TypeDesc as first planned. Simpler and one increment\n  empties every site everywhere; per-desc epochs can come if reloads\n  turn out frequent enough that refilling every site matters.\n- Test: crates/caribou-interop/tests/reload.rs (edit hud.wren, reload,\n  Haxe's bound call reaches the new body; unknown module is Err).\n\nCost of the seventh CallSite word (transfer bench, alternating binaries,\nmedian of 9): same object, fresh, returned and string unchanged in both\ndirections; Wren-\u003eHaxe sequence 97 -\u003e 111 ns per element. A build of\nHEAD with an untouched padding word gives 108, so it is the size, not\nthe check; that path reads no epoch. Packing the epoch into `reentrant`\n(a flag) or `plain` (a counter) was not done.\n\nNot in this increment: Ash reload (`Adapter::reload` is Err for Haxe;\nthe ash bytecode-hash diff, swap_compiled and code freeing), the\ntrigger (file watch, with the reactor a655aa5), before/after hooks,\nReloadPlan/quiescent point (a reload today runs on the world's thread\nbetween ticks, which is the quiescent point for a single-threaded\nprogram; not yet for one with workers), TaskError/Log events.","files":null}]}