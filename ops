{"author":{"id":"3f8bad4585cb0f07c6570d9e31f06264af3b0abf48efbc5a44f56df5e7a9cd45"},"ops":[{"type":3,"timestamp":1789333987,"nonce":"jfwksdXcO+ykG4N6Kv4haiJtuwI=","message":"With ash ccf78c9 (a trap armed in the caller's storage) and wren_lift\na744d04 (`Method::Host`, `call_found_closure`), caribou 569834e and\n00823dd, each cell alone, ns per call:\n\n                Haxe→Haxe  Wren→Wren  Haxe→Wren  Wren→Haxe\n    static call       3.6        3.6       26.5       54.5\n    method call       3.5        5.6       26.9       60.1\n    getter            3.5        3.5       14.7       31.7\n    setter            0.7        3.7       20.5       31.6\n    closure call      3.5        3.6       59.3       64.2\n    construct         7.6       33.9      310.2      277.6\n\nHaxe→Wren static, 26.5: Ash's record call and the entry ~5; the bridge\n~3; the direct send ~9 (thread-local VM ~1.5, class check, the\nargument buffer, the result); `call_found_closure` ~8.6 (two\nthread-local reads, the context save and restore, `func_module` and\n`module_vars_for` in `call_jit_with_shadow_st`); the body ~3.\n\nWren→Haxe static, 54.5, by experiment (each removed in turn): the\nHashLink trap 13 (two thread-local reads, `gc_lock_held_depth`,\n`_setjmp`); `ash_native_call::dispatch` over a direct transmute 2.\nFrom `sample`: wren_lift's `wren_call_1` → `dispatch_call` →\n`dispatch_method` ~14 (three thread-local reads, the argument rooting);\nthe host entry's glue ~7; the bridge ~3; placing the arguments and the\nresult ~8; the body ~4.\n\nLeft, and what each is worth:\n- wren_lift: an inline cache for a host method at a compiled call site,\n  calling the `HostFn` with its context after the class check, as kind\n  4 does for a native in the older codegen (~11 of the 54).\n- Ash: exceptions that unwind through compiled frames (unwind tables\n  from Cranelift and LLVM, `hlp_throw` raising through them) would make\n  the trap free on the happy path (~13 of the 54); short of that, one\n  thread-local read fewer per arm and disarm (~2).\n- caribou: the host entry's glue and the argument placing (~15 of the\n  54, ~12 of the 26), which is now plain code with no lookups; what is\n  left there is register-file arrays, conversions and bounds checks.","files":null}]}